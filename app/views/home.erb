<div class="container">

  <h2> Welcome to URL Shortener!<h2>

    <h3>Login to get all your URL shorties!</h3>
    <form action="/login" method = "post">
     <p>Email : <input type="text" name="login[email]"></p>
     <p>Password : <input type="password" name="login[password]"></p>
     <p><input type="submit" value="login"></p>
    </form>
     <p>Don't have an account? <a href="/signup">Make one now!</a></p>
     <p>Ain't got no time for that? Short a URL now</p>
       <form action="/urls" method="post">
        <p><b>Your URL : </b><input type="text" name="url"></p>
        <p><input type="submit" value="Short it!"></p>
       </form>

       <p>Current shorted url</p>
      <table border=1>
      <tr>
        <th>Url</th>
        <th>Short Url</th>
       </tr>
        <% @shorturl.each do |url|%>
        <% if url.user_id.nil? %>
        <tr>
        <td><%= url.ori_url%></td>
        <td><a href = "/urls/<%= url.short_url%>"><%=url.short_url%></td>
       </tr>
         <% end %>
          <% end %>
    </table>
</div>